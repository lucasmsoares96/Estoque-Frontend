import 'package:flutter/material.dart';

class AdministracaoPage extends StatelessWidget {
  const AdministracaoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Pagina de Administração"),
    );
  }
}
